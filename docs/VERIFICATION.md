# Verification snapshot

Recorded September 10, 2026. These are executed results for the unchanged app,
not a claim that the full roadmap or all BI-1 requirements are complete.

- `pnpm verify`: 56 unit/integration and 14 Chrome tests passed, with no failures,
  skips or retries; lint, typecheck, domain boundaries and build exited 0.
- Real `gpt-6-astra` / Codex CLI 0.154.0 public research returned four candidates
  from Namoki, Alibaba and supporting sources. Browser-to-local-service research,
  plan selection, partial cost, save/reopen and export completed without page errors.
- Separate read-only implementation and repair review addressed reported defects.
- Original development runtime/test/configuration/resource digest:
  `25bc08a5460924b392c077628c1ea863b6fc73b8453397c7bbe4b0682781d611`.

Raw local evidence, provider transcripts and machine paths are not published.
The reproducible commands and checks remain in source; their new runs create
ignored local evidence. Fixture tests are not live supplier/provider certification.

The curated public snapshot was verified again before publication: `pnpm verify`
passed all six commands, with 56 unit/integration and 14 Chrome tests. The first
attempt passed the non-browser checks but the sandbox denied the test server's
local port bind before browser tests ran. Its failed receipt was retained; a
separate permissioned run passed without source, baseline or assertion changes.
This rerun did not invoke the live research provider or requalify the asset branches.

Before publication, the evidence script's personal acceptance label was changed
to `pending owner`. A further complete `pnpm verify` run passed 56 unit/integration
and 14 Chrome tests plus all other checks. The final public source digest is
`02b39ab61983da335a9f7111a825398798c4f122310af8207fcab20010f3b793`.
Application behavior, tests and baselines were not changed by this privacy edit.
The initial public commit was rebuilt before any push so the personal label does
not remain in published history. Asset branches share that clean base and remain
unmerged until the asset work is ready and integration review passes.

## Open gates

Private-image/rendered-preview analysis is absent. Actual HTTP timeout/concurrency,
subprocess failure/isolation and completed/killed-stream accounting negative-test
coverage is incomplete. The service is not production-ready.

Asset branch integration and human product/visual acceptance remain pending.
The main renderer has placeholder materials. Safari and full accessibility/mobile
certification are not claimed. The existing approximately 960 kB uncompressed JS
bundle warning remains; it was not suppressed to pass the build.

Compatibility covers only supported evidence inputs; unknown required interfaces
prevent overall fit approval. Merchandise subtotals exclude unknown remaining
parts, shipping, taxes, labor and testing. Supplier discovery is not endorsement.
