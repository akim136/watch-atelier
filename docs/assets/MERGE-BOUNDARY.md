# Demo asset merge boundary

The supplied branch contained all prior asset branches but failed host typecheck
with 12 errors and unit tests with 79 passed/3 failed. It replaced the current
viewport/factory contract, removed context restoration and carried an older license
hash. Those failures were reproduced before integration; no baseline was weakened.

Bounded resolution: preserve the current main watch/viewport/dial/resources files
byte-for-byte. Isolate ten older renderer modules under `src/render/assets/legacy/`
and mechanically relocate imports in their fixtures and color recipes. New hand,
material and named-model factories remain unchanged. No canonical schema, stored
project, undo/history or research behavior changes. All supplied tests remain.

Registry paths/hashes reflect namespace/import-only changes and retain original
paths/hashes. The existing main OFL file is retained; its text equals the upstream
copy after whitespace normalization. Its actual hash replaces the stale dependency
hash with the prior hash retained explicitly. This is provenance reconciliation,
not a changed visual baseline, font binary or test assertion.

The asset lab is accessible through README's local development links. Model studies
are not persisted designs or sourcing substitutions. Full saved semantic model
selection remains future integration; no visual, physical or production acceptance.

All original M1/BI tests and the added 26 asset unit tests remain required. The full
asset browser suite remains separate, using an explicit isolated local test port.
Independent review verifies host byte parity, no legacy dependency from studio,
preserved tests and evidence before merge. No private development ancestry is used.
